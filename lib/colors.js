import MidiLaunchpad from 'midi-launchpad';
import {LaunchControlXL} from './class/launchControl';

export let LaunchPadColors = MidiLaunchpad.colors;
export let LaunchCtrlColors = LaunchControlXL.colors;

